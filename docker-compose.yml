services:
  campaign-app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - history-data:/app/data
    environment:
      - DB_PATH=/app/data/history.db

volumes:
  history-data:
